\documentclass{standalone}
\usepackage{mintikz}

\begin{document}
\begin{circuitikz}
    \draw
    (0,0)
    to[V, name=E, V^>=$E_{}$, i^>=$i_{}$, !vi]
    (0,2)
    to[closing switch]
    (0,4)
    to[short, name=S, i>^=$i$, !vi]
    (3,4)
    to[R, name=R, i^>=$i$, v^<=$u_R$, !vi]
    (3,2)
    to[L,l_=$L$, name=L, v^<=$u_L$, !vi]
    (3,0) --
    (0,0);
    \iarronly{E} \iarronly{S} \iarronly{R}
    \varronly{E} \varronly{R} \varronly{L}
    \node[] at (R.center) {$R$}
\end{circuitikz}
\end{document}
