\documentclass{standalone}
\usepackage{mintikz}

\begin{document}
\begin{circuitikz}[line width=.7pt]
	\draw
	(0,0)
	to[V, name=E, V^>=$e(t)$, i^>=$i_{}$, !vi]
	(0,2)
	to[R, name=R, v^<=$u_R$, !vi]
	(3,2)
	to[C,l_=$C$, name=C, v^<=$u_C$, !vi]
	(3,0) --
	(0,0);
	\iarronly{E}
	\varronly{E} \varronly{R} \varronly{C}
	\node[] at (R.center) {$R$};
\end{circuitikz}
\end{document}
