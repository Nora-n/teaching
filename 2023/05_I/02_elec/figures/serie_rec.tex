\documentclass{standalone}
\usepackage{mintikz}

\begin{document}
\begin{circuitikz}
	\draw
	(0,0)
	node {$\bullet$}
	to[R, name=R1, i^>=$i$, v^<=$u_1$, !vi]
	(2,0)
	node {$\bullet$}
	coordinate (A)
	to[R, name=R2, i^>=$i$, v^<=$u_2$, !vi]
	(4,0)
	node {$\bullet$};
	\iarronly{R1} \iarronly{R2}
	\varronly{R1} \varronly{R2}
\end{circuitikz}
\end{document}
