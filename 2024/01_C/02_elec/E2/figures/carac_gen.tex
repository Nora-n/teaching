\documentclass{standalone}
\usepackage{mintikz}

\begin{document}
\begin{tikzpicture}[scale=.6]
    \begin{axis}[
        axis lines=center,
        xmin=-2*pi, xmax=2*pi,
        xlabel=$I$, ylabel=$U$,
        clip=false]
        \addplot[
            domain=-2*pi:2*pi,
            smooth,
            color=firebrick]
            {1*\x+3*sin(\x r)-1*sin(.5*\x r)};
        % \addlegendentry{$U_R = RI$}
    \end{axis}
\end{tikzpicture}
\end{document}
