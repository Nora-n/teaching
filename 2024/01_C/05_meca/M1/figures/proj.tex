\documentclass{standalone}
\usepackage{mintikz}

% \toggletrue{student}
% \toggletrue{corrige}

\settype{prof}
\settype{stud}

\begin{document}
\begin{tikzpicture}[scale=1]
	% O
	\coordinate (O) at (0,0);
	\node at (O) [below left=-.07 and -.07]{O};
	% Axes
	\draw [->,-latex, very thick]
	(O) --++ (2.5,0)
	coordinate (R)
	node [below] {$\ux$};
	\draw [->,-latex, very thick]
	coordinate (T)
	(O) --++ (0,2.5) node [left] {$\uy$};
	\ifprof{%
		% sens angles
		\node (Cp) at (2.5,1.5) {$\oplus$};
		\circledarrow{black}{(Cp)}{0.25}
		% Vecteur
		\draw [->,-latex, thick, mygreen]
		(O) --++ (30:2.5)
		coordinate (Atop)
		node [above, midway, sloped] {$\vv{A}$};
		% Projections
		\draw [dashed, myblue, thick]
		(Atop) -- ($(O)!(Atop)!(R)$)
		node[midway, right] {$A_y$}
		coordinate (Ar);
		\draw [dashed, myred, thick]
		(Atop) -- ($(O)!(Atop)!(T)$)
		node[midway, above] {$A_x$}
		coordinate (At);
		\draw [dashed, myred, thick]
		(O) -- (Ar)
		node[midway, below] {$A_x$};
		\draw [dashed, myblue, thick]
		(O) -- (At)
		node[midway, left] {$A_y$};
		% angle
		\pic[angle radius=0.75cm, angle eccentricity=1.25,
			draw, "$\theta$" alias=theta,
			->, color=black]
		{angle=R--O--Atop};
		\pic[angle radius=0.55cm, angle eccentricity=1.25,
			draw, "$\f$" alias=phi,
			->, color=black]
		{angle=Atop--O--T};
	}%
	\sswitch{%
		% Text
		\node[white] (Tr) at (1.55, 2.5)
		{\small$A_x = \vv{A}\!\cdot\!\!\ux = A\cos(\theta)$};
		\node[white, below=-.20 of Tr]
		{\small$A_y = \vv{A}\!\cdot\!\!\uy = A\sin(\theta)$};
	}{%
		% Text
		\node[myred] (Tr) at (1.55, 2.5)
		{\small$A_x = \vv{A}\!\cdot\!\!\ux = A\cos(\theta)$};
		\node[myblue, below=-.20 of Tr]
		{\small$A_y = \vv{A}\!\cdot\!\!\uy = A\sin(\theta)$};
	}%

\end{tikzpicture}
\end{document}
